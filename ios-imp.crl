mod std = import(lib, "std");

i32 main() {
    f64 e = std.math.pi;
        std.libc.printf("The Value of pi is: %f, %d", e, std.math.truncF(e));
    // std.ios.print("to meet girl in park is good, but to..", 2323, 3.24);

    return 0;
}