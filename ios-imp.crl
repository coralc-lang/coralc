
extern i64 truncF(f64);
extern i32 printf(const char* fmt, ...);

i32 main() {
    f64 e = 3.14;
        printf("The Value of pi is: %f, %llu", e, truncF(e));
    // std.ios.print("to meet girl in park is good, but to..", 2323, 3.24);

    return 0;
}