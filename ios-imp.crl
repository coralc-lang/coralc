

f64 pow(f64 base, i64 exponent)
{
    f64 count = 1;

    for (i32 i = 0; i < exponent; i++)
    {
        count += base;
    }

    return count;
}

extern i32 printf(const char* fmt, ...);

i32 main() {

    printf("%f", pow(2, 10));
    // std.ios.nprint("\t32232\t");
    return 0;
}