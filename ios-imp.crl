extern i32 write(i32 fd, const char* msg, u64 len);
extern u64 strlen(const char*);

//extern i32 write()

void println(const char* msg)
{
    if (strlen(msg) > 0) write(1, msg, strlen(msg));
    write(1, "\n", 1);
}

i32 main() {
    println("\t32232\t");
    return 0;
}