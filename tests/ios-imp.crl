mod std = import(lib, "std");

i32 main() {
    f64 e = 3.14;
    var v = std.string.from("whos there");

    std.ios.print(v);
    // std.ios.print("to meet girl in park is good, but to..", 2323, 3.24);

    return 0;
}