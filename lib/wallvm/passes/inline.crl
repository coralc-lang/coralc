@[no_warn_unsafe]


mod ir_types = import("wallvm/ir_types.crl");
mod ir_opcodes = import("wallvm/ir_opcodes.crl");
mod mman = import("std/memory/mman.crl");

// Simple key-value mapping table for value remapping during cloning
struct ValMap
{
    ir_types.IrValue** keys;
    ir_types.IrValue** vals;
    u32 count;
    u32 cap;

    void init()
    {
        self.keys = null;
        self.vals = null;
        self.count = 0;
        self.cap = 0;
    }

    void put(ir_types.IrValue* k, ir_types.IrValue* v)
    {
        if (self.count >= self.cap)
        {
            u32 nc = self.cap ? self.cap * 2 : 32;
            ir_types.IrValue** nk = mman.alloc<ir_types.IrValue*>(nc);
            ir_types.IrValue** nv = mman.alloc<ir_types.IrValue*>(nc);
            for (u32 i = 0; i < self.count; i++)
            {
                nk[i] = self.keys[i];
                nv[i] = self.vals[i];
            }
            if (self.keys) mman.dealloc<ir_types.IrValue*>(self.keys);
            if (self.vals) mman.dealloc<ir_types.IrValue*>(self.vals);
            self.keys = nk;
            self.vals = nv;
            self.cap = nc;
        }
        self.keys[self.count] = k;
        self.vals[self.count] = v;
        self.count++;
    }

    ir_types.IrValue* get(ir_types.IrValue* k)
    {
        for (u32 i = 0; i < self.count; i++)
            if (self.keys[i] == k)
                return self.vals[i];
        return null;
    }

    void free()
    {
        if (self.keys) mman.dealloc<ir_types.IrValue*>(self.keys);
        if (self.vals) mman.dealloc<ir_types.IrValue*>(self.vals);
        self.keys = null;
        self.vals = null;
        self.count = 0;
        self.cap = 0;
    }
};

static u32 countBodyInsts(ir_types.IrFunc* f)
{
    u32 n = 0;
    u32 skip = f.nparams;
    for (u32 bi = 0; bi < f.nblocks; bi++)
    {
        ir_types.IrBlock* bb = f.blocks[bi];
        u32 start = (bi == 0) ? skip : 0;
        if (start > bb.ninsts) start = bb.ninsts;
        n += bb.ninsts - start;
    }
    return n;
}

static ir_types.IrInst* cloneInst(ir_types.IrInst* old, ir_types.IrBlock* newBlock, ValMap* valMap, ValMap* blkMap, ir_types.IrContext* ctx)
{
    ir_types.IrValue** newOps = mman.alloc<ir_types.IrValue*>(old.nops > 0 ? old.nops : 1);

    for (u32 i = 0; i < old.nops; i++)
    {
        ir_types.IrValue* op = old.ops[i];
        if (!op) { newOps[i] = null; continue; }

        if (op.kind == ir_types.IrValueKind::Inst)
        {
            ir_types.IrValue* m = valMap.get(op);
            if (m) { newOps[i] = m; continue; }
        }
        else if (op.kind == ir_types.IrValueKind::Block)
        {
            ir_types.IrValue* m = blkMap.get(op);
            if (m) { newOps[i] = m; continue; }
        }

        newOps[i] = op;
    }

    ir_types.IrInst* ni = ctx.createInst(old.opcode, old.base.type, newOps, old.nops,
                                (u64)old.icmpPred, (u64)old.fcmpPred);

    newBlock.addInst(ni);

    valMap.put(&old.base, &ni.base);

    for (u32 i = 0; i < old.nops; i++)
        if (newOps[i])
            newOps[i].addUser(ni);

    return ni;
}

static void eraseInstAt(ir_types.IrBlock* bb, u32 ii)
{
    for (u32 j = ii; j < bb.ninsts - 1; j++)
        bb.insts[j] = bb.insts[j + 1];
    bb.ninsts--;
}

static void transferSuccs(ir_types.IrBlock* src, ir_types.IrBlock* dst)
{
    for (u32 i = 0; i < src.nsuccs; i++)
    {
        ir_types.IrBlock* succ = src.succs[i];
        succ.removePred(src);
        dst.addSucc(succ);
        for (u32 pi = 0; pi < succ.ninsts; pi++)
        {
            ir_types.IrInst* phi = succ.insts[pi];
            if (phi.opcode == ir_opcodes.IrOpcode::Phi)
            {
                for (u32 oi = 1; oi < phi.nops; oi += 2)
                    if (phi.ops[oi] == &src.base)
                        phi.ops[oi] = &dst.base;
            }
        }
    }
    src.nsuccs = 0;
    if (src.succs)
    {
        mman.dealloc<ir_types.IrBlock*>(src.succs);
        src.succs = null;
    }
    src.capSuccs = 0;
}

static void moveInstsAfter(ir_types.IrBlock* src, u32 startIdx, ir_types.IrBlock* dst)
{
    u32 nToMove = src.ninsts - startIdx;
    for (u32 i = 0; i < nToMove; i++)
    {
        ir_types.IrInst* inst = src.insts[startIdx];
        eraseInstAt(src, startIdx);
        inst.parent = dst;
        dst.addInst(inst);
    }
}

static bool inlineCall(ir_types.IrFunc* caller, ir_types.IrBlock* callBlock, ir_types.IrInst* callInst,
                        u32 instIdx, ir_types.IrFunc* callee, ir_types.IrContext* ctx)
{
    u32 nCallArgs = (callInst.nops > 0) ? callInst.nops - 1 : 0;
    ir_types.IrValue** callArgs = (callInst.nops > 1) ? &callInst.ops[1] : null;

    ir_types.IrBlock* afterBlock = ctx.createBlock("after_inline");
    caller.addBlock(afterBlock);

    transferSuccs(callBlock, afterBlock);

    moveInstsAfter(callBlock, instIdx + 1, afterBlock);

    for (u32 i = 0; i < callInst.nops; i++)
        if (callInst.ops[i])
            callInst.ops[i].removeUser(callInst);
    eraseInstAt(callBlock, instIdx);

    ValMap blkMap;
    blkMap.init();

    ir_types.IrBlock** newBlocks = mman.alloc<ir_types.IrBlock*>(callee.nblocks);
    for (u32 bi = 0; bi < callee.nblocks; bi++)
    {
        ir_types.IrBlock* ob = callee.blocks[bi];
        ir_types.IrBlock* nb = ctx.createBlock(ob.base.name);
        caller.addBlock(nb);
        newBlocks[bi] = nb;
        blkMap.put(&ob.base, &nb.base);
    }
    ir_types.IrBlock* calleeEntry = newBlocks[0];

    ValMap valMap;
    valMap.init();

    u32 nparams = callee.nparams;
    ir_types.IrBlock* oldEntry = callee.blocks[0];
    for (u32 i = 0; i < nparams && i < nCallArgs; i++)
    {
        if (i < oldEntry.ninsts)
        {
            ir_types.IrInst* paramInst = oldEntry.insts[i];
            valMap.put(&paramInst.base, callArgs[i]);
        }
    }

    for (u32 bi = 0; bi < callee.nblocks; bi++)
    {
        ir_types.IrBlock* ob = callee.blocks[bi];
        ir_types.IrBlock* nb = newBlocks[bi];
        u32 start = (bi == 0) ? nparams : 0;

        for (u32 ii = start; ii < ob.ninsts; ii++)
            cloneInst(ob.insts[ii], nb, &valMap, &blkMap, ctx);
    }

    for (u32 bi = 0; bi < callee.nblocks; bi++)
    {
        ir_types.IrBlock* nb = newBlocks[bi];
        for (u32 ii = 0; ii < nb.ninsts; ii++)
        {
            ir_types.IrInst* ni = nb.insts[ii];
            for (u32 j = 0; j < ni.nops; j++)
            {
                ir_types.IrValue* op = ni.ops[j];
                if (!op || op.kind != ir_types.IrValueKind::Inst) continue;
                ir_types.IrValue* m = valMap.get(op);
                if (!m || m == op) continue;
                op.removeUser(ni);
                ni.ops[j] = m;
                m.addUser(ni);
            }
        }
    }

    // Step 5, Wire up control flow
    // Add br from callBlock to callee entry
    {
        ir_types.IrValue** brOps = mman.alloc<ir_types.IrValue*>(1);
        ir_types.IrValue* entryVal = &calleeEntry.base;
        brOps[0] = entryVal;
        ir_types.IrInst* brInst = ctx.createInst(ir_opcodes.IrOpcode::Br, ctx.tyVoid(), brOps, 1, 0, 0);
        mman.dealloc<ir_types.IrValue*>(brOps);
        callBlock.addInst(brInst);
        entryVal.addUser(brInst);
        callBlock.addSucc(calleeEntry);
    }

    // Step 6, Handle return values
    // Collect all ret instructions in the cloned callee blocks
    u32 retCount = 0;
    ir_types.IrInst** retInsts = mman.alloc<ir_types.IrInst*>(callee.nblocks);
    for (u32 bi = 0; bi < callee.nblocks; bi++)
    {
        ir_types.IrBlock* nb = newBlocks[bi];
        for (u32 ii = 0; ii < nb.ninsts; ii++)
        {
            if (nb.insts[ii].opcode == ir_opcodes.IrOpcode::Ret)
                retInsts[retCount++] = nb.insts[ii];
        }
    }

    // Create continuation block
    ir_types.IrBlock* contBlock = ctx.createBlock("cont_inline");
    caller.addBlock(contBlock);

    bool hasRetVal = callInst.base.type && callInst.base.type.kind != ir_types.IrTypeKind::Void;

    for (u32 ri = 0; ri < retCount; ri++)
    {
        ir_types.IrInst* ret = retInsts[ri];
        ir_types.IrBlock* retBlock = ret.parent;

        // Remove ret from user lists
        for (u32 oi = 0; oi < ret.nops; oi++)
            if (ret.ops[oi])
                ret.ops[oi].removeUser(ret);

        // Replace ret with br to contBlock
        // first, remove ret from retBlock's inst list
        for (u32 ii = 0; ii < retBlock.ninsts; ii++)
        {
            if (retBlock.insts[ii] == ret)
            {
                eraseInstAt(retBlock, ii);
                break;
            }
        }

        // Add successor edge(goon) from retBlock to contBlock
        retBlock.addSucc(contBlock);

        // Create br to contBlock
        ir_types.IrValue** brOps = mman.alloc<ir_types.IrValue*>(1);
        ir_types.IrValue* contVal = &contBlock.base;
        brOps[0] = contVal;
        ir_types.IrInst* brInst = ctx.createInst(ir_opcodes.IrOpcode::Br, ctx.tyVoid(), brOps, 1, 0, 0);
        mman.dealloc<ir_types.IrValue*>(brOps);
        retBlock.addInst(brInst);
        contVal.addUser(brInst);
    }

    // Create phi at continuation if multiple return sites and non-void
    ir_types.IrValue* mergedRetVal = null;

    if (hasRetVal && retCount > 1)
    {
        ir_types.IrType* retTy = callInst.base.type;
        ir_types.IrValue** phiVals = mman.alloc<ir_types.IrValue*>(retCount);
        ir_types.IrValue** phiBlks = mman.alloc<ir_types.IrValue*>(retCount);
        u32 phiCount = 0;

        for (u32 ri = 0; ri < retCount; ri++)
        {
            ir_types.IrInst* ret = retInsts[ri];
            if (ret.nops > 0 && ret.ops[0])
            {
                ir_types.IrBlock* retBlock = ret.parent;
                phiVals[phiCount] = ret.ops[0];
                phiBlks[phiCount] = &retBlock.base;
                phiCount++;
            }
        }

        if (phiCount > 1)
        {
            ir_types.IrValue** phiOps = mman.alloc<ir_types.IrValue*>(phiCount * 2);
            for (u32 i = 0; i < phiCount; i++)
            {
                phiOps[i * 2] = phiVals[i];
                phiOps[i * 2 + 1] = phiBlks[i];
            }
            ir_types.IrInst* phiInst = ctx.createInst(ir_opcodes.IrOpcode::Phi, retTy, phiOps, phiCount * 2, 0, 0);
            contBlock.addInst(phiInst);
            for (u32 i = 0; i < phiCount * 2; i++)
                if (phiOps[i])
                    phiOps[i].addUser(phiInst);
            mman.dealloc<ir_types.IrValue*>(phiOps);
            mergedRetVal = &phiInst.base;
        }
        else if (phiCount == 1)
        {
            mergedRetVal = phiVals[0];
        }

        if (phiVals) mman.dealloc<ir_types.IrValue*>(phiVals);
        if (phiBlks) mman.dealloc<ir_types.IrValue*>(phiBlks);
    }
    else if (hasRetVal && retCount == 1)
    {
        ir_types.IrInst* ret = retInsts[0];
        if (ret.nops > 0 && ret.ops[0])
            mergedRetVal = ret.ops[0];
    }

    // Free cloned ret instructions (no longer needed)
    for (u32 ri = 0; ri < retCount; ri++)
        retInsts[ri].erase();

    // Add br from contBlock to afterBlock
    {
        ir_types.IrValue** brOps = mman.alloc<ir_types.IrValue*>(1);
        ir_types.IrValue* afterVal = &afterBlock.base;
        brOps[0] = afterVal;
        ir_types.IrInst* brInst = ctx.createInst(ir_opcodes.IrOpcode::Br, ctx.tyVoid(), brOps, 1, 0, 0);
        mman.dealloc<ir_types.IrValue*>(brOps);
        contBlock.addInst(brInst);
        afterVal.addUser(brInst);
        contBlock.addSucc(afterBlock);
    }

    // Step 7, Replace uses of call result with the merged return value
    if (hasRetVal && mergedRetVal)
    {
        ir_types.IrValue* callVal = &callInst.base;
        callVal.replaceAllUses(mergedRetVal);
    }

    // Cleanup dat biiiiiih
    // Fully erase the call instruction (frees ops, users, and the struct)
    callInst.erase();

    mman.dealloc<ir_types.IrBlock*>(newBlocks);
    mman.dealloc<ir_types.IrInst*>(retInsts);

    valMap.free();
    blkMap.free();

    return true;
}

pub bool irPassInline(ir_types.IrFunc* func, void* userdata)
{
    ir_types.IrContext* ctx = (ir_types.IrContext*)userdata;
    if (!ctx || !func) return false;
    if (func.nblocks == 0) return false;

    const u32 MAX_INLINE_SIZE = 20;
    bool changed = false;

    // Iterate looking for inlineable calls. After each successful inline
    // the CFG changes, so we restart scanning from the beginning
    bool progress = true;
    while (progress)
    {
        progress = false;

        for (u32 bi = 0; bi < func.nblocks; bi++)
        {
            ir_types.IrBlock* bb = func.blocks[bi];

            for (u32 ii = 0; ii < bb.ninsts; ii++)
            {
                ir_types.IrInst* inst = bb.insts[ii];
                if (inst.opcode != ir_opcodes.IrOpcode::Call) continue;

                // Callee must be at ops[0] (convention used by all backends)
                if (inst.nops < 1) continue;
                ir_types.IrValue* calleeVal = inst.ops[0];
                if (!calleeVal || calleeVal.kind != ir_types.IrValueKind::Func) continue;

                ir_types.IrFunc* callee = (ir_types.IrFunc*)calleeVal;

                // Do not inline extern functions or recursive calls
                if (callee.isExtern || callee == func) continue;

                // Cost model, only inline small functions
                if (countBodyInsts(callee) >= MAX_INLINE_SIZE) continue;

                // Inline the call
                if (inlineCall(func, bb, inst, ii, callee, ctx))
                {
                    changed = true;
                    progress = true;
                    break; // restart scanning
                }
            }

            if (progress) break;
        }
    }
    return changed;
}
