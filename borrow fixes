1. Better region tracking through the union-find (connecting alloca→GEP→bitcast chains)
2. Call-argument alias analysis (two args pointing to same base)
3. DebugLoc propagation for precise file:line:col diagnostics
4. Distinct messages per rule instead of one generic string

Borrow checking status: The pass runs (confirmed via trace) but doesn't detect even clear aliasing violations. It's wired into both irStructureLane (always) and emits.crl (when --fborrowcheck). To enhance it needs:
1. Better region tracking through the union-find (connecting alloca→GEP→bitcast chains)
2. Call-argument alias analysis (two args pointing to same base)
3. DebugLoc propagation for precise file:line:col diagnostics
4. Distinct messages per rule instead of one generic string

Ours vs C gap: 98 vs 69 lines for ios-imp. Remaining overhead is: double-indirection through alloca for struct fields (mem2reg should collapse), multi-arg push/pop staging, and frame over-allocation (56 vs 16 bytes).

Remaining known gaps (next session):
- Loop-carried phi resolution (needs proper phi elimination or lsra back-edge fix)
- Multi-arg direct-mov interference analysis
- Frame packing (spill slots + allocas share space)
- Borrow checker enhancement (pass exists but doesn't detect aliasing)

lowerphis never really created.